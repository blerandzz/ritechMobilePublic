const config = {
  perfectoToken: process.env.PERFECTO_SECURITY_TOKEN || '',
  testUser: process.env.TEST_USER || '',
  testPassword: process.env.TEST_PASSWORD || '',
  testUsername: process.env.TEST_USERNAME || '',
};
export default config;
